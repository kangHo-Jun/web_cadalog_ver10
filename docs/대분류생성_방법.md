[상품 데이터 출처]
- 상품 조회는 `src/app/api/products/route.ts:22-27`, `src/app/api/products/route.ts:69-75`에서 `apiClient.get('/products', ...)`로 Cafe24 API 호출.
- 견적/가격 모드에서 조회 대상 카테고리는 `src/app/api/products/route.ts:18`의 `category ? [Number(category)] : QUOTE_CATEGORY_NOS`.
- `QUOTE_CATEGORY_NOS`는 `src/config/quote-categories.ts:84`에서 `QUOTE_CATEGORIES.map(c => c.category_no)`로 생성.
- 정적 상품 파일(`products_data.js`)은 코드 참조 없음(`rg` 검색 결과 import/사용 위치 없음).

[신규 상품 등록 절차]
1. Cafe24 관리자에서 상품 등록.
2. 등록 상품을 카테고리 번호 `325`에 연결.
3. 등록 상품이 판매 상태 `selling='T'` 조건을 만족해야 현재 API 조회 대상(`src/app/api/products/route.ts:26`, `src/app/api/products/route.ts:73`).
4. 화면은 `activeCategoryNo`를 `category`로 전달해 조회(`src/components/QuoteCatalog.tsx:319`, `src/components/QuoteCatalog.tsx:403`), 서버는 `/api/products`에서 해당 카테고리로 Cafe24 재조회(`src/app/api/products/route.ts:9-27`).

[코드 수정 필요 여부]
- 신규 상품을 기존 `325`에 추가: **불필요**
- 신규 카테고리 번호 자체를 추가해서 조회 대상에 포함: **필요**
- 필요 시 파일:
  - `src/config/quote-categories.ts` (`QUOTE_CATEGORIES`, `QUOTE_CATEGORY_NOS`)
  - `src/app/api/categories/route.ts` (견적 카테고리 응답에 `QUOTE_CATEGORIES` 사용: `src/app/api/categories/route.ts:16-19`)
