# 카테고리 데이터 현황

## 검색 결과

### 발견된 파일

#### 1. categories_data.js
**위치:** `/categories_data.js`  
**내용:**
```javascript
const categories = [
  { "category_no": 50, "category_name": "석고보드" },
  { "category_no": 26, "category_name": "단열재" },
  { "category_no": 25, "category_name": "각재/목재" }
];
```

**특징:**
- 총 3개 카테고리
- `category_no`: 25, 26, 50
- `category_depth`, `parent_category_no` 필드 없음

#### 2. categories_dump.json
**위치:** `/categories_dump.json`  
**내용:** 빈 파일 (0 bytes)

#### 3. test/전체.csv
**위치:** `/test/전체.csv`  
**내용:** 상품 데이터 (491 rows, 5MB)  
**문제:** 카테고리 구조 정보 없음, "상품분류 번호" 필드가 HTML과 혼재

#### 4. docs/Test/테스트.xlsx
**위치:** `/docs/Test/테스트.xlsx`  
**내용:** 상품 데이터 (Excel 2007+)  
**헤더:** 상품명, 상품코드, 옵션 스타일, 품목명, 품목코드 등  
**문제:** 카테고리 구조 정보 없음

---

## 사용 가능한 카테고리 데이터

### categories_data.js (유일한 카테고리 데이터)

| category_no | category_name |
|-------------|---------------|
| 50 | 석고보드 |
| 26 | 단열재 |
| 25 | 각재/목재 |

### 누락된 정보
- `category_depth` (카테고리 깊이)
- `parent_category_no` (부모 카테고리 번호)
- 중분류 카테고리
- 합판/MDF, 방부목/데크, 집성목/계단, 철물/부자재 등 기타 카테고리

---

## 하드코딩 구조와 비교

### QUOTE_CATEGORIES (하드코딩)
```
325 - 단열재(견적서)
326 - 석고보드(견적서)
327 - 각재/목재(견적서)
328 - 합판/MDF(견적서)
329 - 방부목/데크(견적서)
330 - 집성목/계단(견적서)
331 - 철물/부자재(견적서)
332 - 소송(견적서) [parent: 327]
333 - 뉴송(견적서) [parent: 327]
```

### categories_data.js (실제 DB)
```
50 - 석고보드
26 - 단열재
25 - 각재/목재
```

### 차이점
1. **카테고리 번호 불일치**: 25/26/50 vs 325/326/327
2. **카테고리 개수**: 3개 vs 9개
3. **계층 구조**: 없음 vs 명시적 depth/parent 구조

---

## 결론

**현재 프로젝트 내 카테고리 데이터:**
- `categories_data.js`: 3개 카테고리만 존재 (25, 26, 50)
- 계층 구조 정보 없음
- Cafe24 실제 카테고리 구조 확인 불가

**필요한 작업:**
- Cafe24 API 인증 설정 후 `/api/categories` 호출
- 또는 Cafe24 관리자에서 **분류관리 → 카테고리 목록** 다운로드
