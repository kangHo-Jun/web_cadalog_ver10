# 웹카다로그 오류 히스토리

## 2026-03-13

### 09:00 - Application error (클라이언트)
- 증상: 사이트 접속 시 Application error
- 원인: undefined 상태에서 .length 호출
- 해결: 옵셔널 체이닝 추가, useState([]) 초기값 설정

### 09:30 - Redis 연결 🔴 + 401 invalid_client_secret
- 증상: Redis 연결 오류 + /api/products 401
- 원인: Redis 환경변수명 불일치 (REDIS_URL vs KV_REDIS_URL)
- 해결: Vercel에 KV_REDIS_URL 올바른 이름으로 재등록

### 10:00 - 토큰 만료 401
- 증상: invalid_client_secret 401
- 원인: Redis에 토큰 없음 → 갱신 시도 → client_secret 불일치
- 해결: reauth_cafe24.py 실행 → Redis 직접 저장

### 14:00 - debug-snapshot 구조 변경으로 프론트 오류
- 증상: Cannot read properties of undefined (reading 'length')
- 원인: API 응답에 lastSnapshot 추가 후 프론트 미반영
- 해결: Object.values(data) → Object.values(data.lastSnapshot ?? data)

### 14:30 - GAS checkTokenExpiry 트리거 미등록
- 증상: 자동 갱신 트리거 등록됐으나 함수 없음
- 원인: createTrigger()에 트리거만 추가, 실제 함수 누락
- 해결: checkTokenExpiry 함수 gas-push/Code.js에 추가

### 16:27 - Bandwidth quota exceeded
- 증상: Redis 연결 🔴 Bandwidth quota exceeded
- 원인: GAS에서 debug-snapshot을 2개 함수가 각각 호출
- 해결: 1회 호출 후 결과 공유하도록 최적화

### 21:28 - 401 invalid_client_secret 재발생
- 증상: /api/products 401
- 원인: CAFE24_CLIENT_SECRET Vercel 환경변수 잘못된 값
- 해결: 파트너센터 값으로 재설정

---

## 2026-03-16

### 재시도 로직 504 타임아웃
- 증상: FUNCTION_INVOCATION_TIMEOUT 504
- 원인: 재시도 로직 15분×3회 = 45분 대기 → Vercel 타임아웃
- 해결: 재시도 로직 제거 → 실패 시 즉시 이메일 발송으로 변경

---

## 2026-03-17

### 09:32 - 504 타임아웃 + 500 오류
- 증상: FUNCTION_INVOCATION_TIMEOUT 504
- 원인: 재시도 로직 미제거 상태에서 토큰 만료
- 해결: 재시도 로직 제거 완료 + 토큰 재발급

---

## 반복 오류 패턴 요약

| 오류 | 발생 횟수 | 근본 원인 |
|------|----------|----------|
| 401 invalid_client_secret | 5회 | 토큰 만료 또는 CLIENT_SECRET 불일치 |
| Redis 연결 오류 | 2회 | 환경변수명 불일치 또는 토큰 유실 |
| 504 타임아웃 | 2회 | 재시도 로직 Vercel 타임아웃 |
| 프론트 .length 오류 | 1회 | API 응답 구조 변경 후 프론트 미반영 |

---

## 현재 자동화 상태 (2026-03-17 기준)

| 항목 | 상태 |
|------|------|
| access_token 자동 갱신 | ✅ 1시간마다 GAS 트리거 |
| Redis 유실 자동 복구 | ✅ 환경변수 fallback |
| refresh_token 알림 | ✅ 만료 1일 전 이메일 |
| CLIENT_SECRET 문제 | ⚠️ 수동 개입 필요 |

## 2026-03-17 (계속)

### 22:32 - 504 타임아웃 재발생
- 증상: FUNCTION_INVOCATION_TIMEOUT 504
- 원인: refreshAccessToken() → saveTokens() → 
  Vercel 환경변수 업데이트 API await 호출
  → 전체 체인이 너무 오래 걸려 Vercel 타임아웃
- 해결: updateVercelEnv() await 제거 → 백그라운드 실행으로 변경
- 파일: src/lib/token-store.ts

---

## 2026-03-18

### 08:00 - 504 타임아웃 재발생
- 증상: FUNCTION_INVOCATION_TIMEOUT 504
- 원인: Vercel 서버(미국) → Cafe24 서버(한국) 
  거리로 인한 응답 지연 → 기본 타임아웃 초과
- 확인: 로컬에서는 refreshAccessToken() 정상 성공
  → 카페24 로그인 문제 아님
  → Vercel 함수 타임아웃 문제
- 해결: vercel.json에 maxDuration: 30 추가
  check-token-expiry 함수 타임아웃 30초로 연장
- 파일: vercel.json

---

### 2026-03-18 10:28 - 504 타임아웃 재발생 (4번째)

- 증상: FUNCTION_INVOCATION_TIMEOUT 504
- 원인: vercel.json에 check-token-expiry cron이 
  자정(0 0 * * *)에 실행되면서
  GAS 트리거와 충돌 + maxDuration 경로 오류
  (src/ 포함 경로가 Next.js App Router와 불일치)
- 해결:
  1. functions 경로 수정: 
     "src/app/api/..." → "app/api/..."
  2. check-token-expiry Vercel cron 제거
     (GAS 트리거 1시간마다로 대체)
- 파일: vercel.json

---
