# Cafe24 OAuth 토큰 관리 가이드

최종 업데이트: 2026-05-06

---

## 1. 핵심 정책 (Cafe24 공식)

| 항목 | 내용 |
|---|---|
| access_token 유효시간 | 약 2시간 |
| refresh_token 유효시간 | 최대 14일 (고정, 연장 불가) |
| refresh 요청 결과 | 새 access_token + 새 refresh_token 세트 반환 |
| 기존 refresh_token | 즉시 폐기 (교체 모델, 재사용 불가) |
| refresh_token 만료 시 | authorization_code 재발급 필요 (자동 복구 불가) |

---

## 2. 먼저 이해해야 할 전체 구조

이 프로젝트는 **구글시트 2개 + GAS 스크립트 2개 + Redis/Vercel** 이 함께 움직입니다.  
초보자가 가장 많이 헷갈리는 부분은 `monitoring-gas` 와 `gas-push(p7_gas)` 를 같은 역할로 오해하는 것입니다.

### 2.1 시트 2개

| 이름 | Spreadsheet ID | 역할 |
|---|---|---|
| 매핑테이블 시트 | `1_T_pl2ItqfmdAsDmrjkg1BBZyQMAVXkUrPMEwhGI6ek` | 상품 매핑, 가격 동기화, `gas-push` 작업용 메인 시트 |
| 모니터링 시트 | `1UnhOYjQmCD-rkYHZv9oP9yuUP0cUJmxV0ERhPK6lpjU` | 토큰 상태, 대시보드, 로그, `monitoring-gas` 작업용 시트 |

### 2.2 GAS 스크립트 2개

| 스크립트 | 연결 위치 | 역할 |
|---|---|---|
| `monitoring-gas/Code.js` | 모니터링 시트 | **현재 토큰 갱신 단일 주체**. `refreshCafe24Token()` 실행, 대시보드/로그 관리 |
| `gas-push/Code.js` (`p7_gas`) | 매핑테이블 시트 | 상품/가격 동기화 주체. 토큰은 읽기 전용에 가깝고, 필요 시 폴백 갱신만 수행 |

### 2.3 저장소 3개

| 저장 위치 | 역할 | 비고 |
|---|---|---|
| 모니터링 시트 `[설정]` | GAS가 직접 읽는 토큰 원본 | `monitoring-gas`가 직접 읽고 직접 갱신 |
| Redis `cafe24_tokens` | 운영 API의 메인 토큰 저장소 | Next.js/Vercel 서버가 우선 사용 |
| Vercel env | Redis 유실 대비 fallback | `CAFE24_ACCESS_TOKEN`, `CAFE24_REFRESH_TOKEN` |

### 2.4 핵심 원칙

1. **토큰 갱신 단일 주체는 `monitoring-gas`**
2. `gas-push`는 메인 갱신 주체가 아니라 **폴백 소비자**
3. 운영 API는 **Redis 우선**, 없으면 **Vercel env fallback**
4. 초보자는 `Code.gs:527` 같은 오류가 나오면 먼저 **어느 GAS 프로젝트를 보고 있는지** 확인해야 함

### 2.5 초보자가 특히 헷갈리는 포인트

- `monitoring-gas`와 `gas-push`는 둘 다 GAS지만 역할이 다름
- 토큰 원본은 현재 **모니터링 시트 `[설정]` 탭**
- 대시보드 표시(`🔴만료`, `🟢정상`)와 실제 토큰 값은 항상 동시에 바뀌지 않을 수 있음
  - 실제 토큰 갱신: `refreshCafe24Token()`
  - 대시보드 갱신: `checkAll()`
- 따라서 `[설정]` 탭 값은 최신인데 대시보드가 예전 `🔴만료`일 수 있음

---

## 3. 단순화 배경 (2026-03 작업)

### 문제: 갱신 주체 분산 → Race Condition 위험

단순화 이전에는 3곳에서 동시에 토큰 갱신 가능:

| 주체 | 방식 | 문제 |
|---|---|---|
| monitoring-gas `refreshCafe24Token()` | 1시간 고정 트리거 | - |
| p7_gas `autoRefreshCafe24Token()` | 1시간 고정 트리거 | 동일 refresh_token 동시 소비 가능 |
| p7_gas `buildCafe24Cache()` | 내부 직접 호출 | 동일 refresh_token 동시 소비 가능 |
| Vercel `check-token-expiry` cron | Vercel cron (미등록) | 불필요한 갱신 로직 보유 |

Cafe24의 교체 모델 특성상 두 주체가 동시에 동일 refresh_token으로 갱신을 시도하면 하나는 `invalid_grant` 실패. 결과적으로 새 token 세트가 서로 다른 저장소에 분리 저장되어 불일치 발생.

### 해결: 단일 갱신 주체 (monitoring-gas)

**삭제된 것:**

1. `p7_gas.js` — `autoRefreshCafe24Token()` 트리거 등록 코드 (`createTrigger()` 내부)
2. `p7_gas.js` — `buildCafe24Cache()` 내부 `autoRefreshCafe24Token()` 직접 호출
3. `vercel.json` — `check-token-expiry` cron 항목
4. `src/app/api/cron/check-token-expiry/route.ts` — access_token 자동 갱신 블록

**유지된 것:**

1. `monitoring-gas/Code.js` — `refreshCafe24Token()` 1시간 트리거 (갱신 단일 주체)
2. `p7_gas.js` — `ensureTokenRefreshIfNeeded_()` (만료 2시간 이내 폴백)
3. `p7_gas.js` — `tryRefreshAndRetry_()` (401 감지 시 긴급 폴백)
4. `src/app/api/cron/check-token-expiry/route.ts` — refresh_token 만료 알림 로직

---

## 4. 토큰 저장 구조

### Redis (메인 저장소)

Key: `cafe24_tokens`

```json
{
  "access_token": "...",
  "refresh_token": "...",
  "expires_at": 1700000000000,
  "refresh_token_expires_at": 1701200000000
}
```

- `expires_at`: Unix ms (now + 2시간)
- `refresh_token_expires_at`: Unix ms (카페24 응답의 ISO 문자열 파싱)

### Vercel 환경변수 (Redis 유실 대비 fallback)

- `CAFE24_ACCESS_TOKEN`
- `CAFE24_REFRESH_TOKEN`
- monitoring-gas가 갱신 시 `/api/token-update`로 동기화

### 구글 시트 [설정] (GAS용 저장소)

- 모니터링 시트 [설정]: monitoring-gas가 직접 읽고 씀
- 매핑테이블 시트 [설정]: p7_gas가 `initMonitoringSheet_()`로 모니터링 시트에서 오버레이

---

## 5. 현재 토큰 갱신 구조

### 단일 갱신 주체: monitoring-gas

```
monitoring-gas (1시간 고정 트리거)
  refreshCafe24Token()
    → 모니터링 시트 [설정]에서 refresh_token 로드
    → 카페24 API 호출 (refresh_token grant)
    → 새 token 세트 수신
    ├─ 모니터링 시트 [설정] 업데이트
    └─ POST https://web-cadalog-ver10.vercel.app/api/token-update
         → Redis 저장
         → Vercel 환경변수 업데이트
```

### p7_gas (읽기 전용 + 폴백)

```
각 함수 실행 시작
  → initMonitoringSheet_() → 모니터링 시트에서 최신 토큰 오버레이
  → G_TOKEN 설정

폴백 1 — ensureTokenRefreshIfNeeded_()
  조건: access_token 만료 2시간 이내
  → 직접 갱신 후 모니터링 시트 + 매핑테이블 시트 저장

폴백 2 — tryRefreshAndRetry_()
  조건: 카페24 API 401 응답 감지
  1차: 모니터링 시트 재읽기 (monitoring-gas가 이미 갱신했을 수 있음)
  2차: 직접 refresh_token으로 갱신
```

### Vercel check-token-expiry (알림 전용)

- `/api/cron/check-token-expiry`: 갱신 로직 없음, 알림 전용
- vercel.json cron 미등록 → 현재 자동 실행 안 됨 (수동 호출만 가능)
- refresh_token 만료 D-1 이하: 이메일 알림 발송

---

## 6. 갱신 주기 정리

| 주체 | 주기 | 역할 |
|---|---|---|
| monitoring-gas `refreshCafe24Token()` | 1시간 고정 | **토큰 갱신 단일 주체** |
| p7_gas `ensureTokenRefreshIfNeeded_()` | 각 함수 실행 시 | 만료 2시간 이내 폴백 갱신 |
| p7_gas `tryRefreshAndRetry_()` | 401 감지 시 | 긴급 폴백 갱신 |
| Vercel `check-token-expiry` | 미예약 (수동) | refresh_token 만료 알림 |

---

## 7. Refresh 요청 방식 (카페24 공식 API)

```
POST https://{mall_id}.cafe24api.com/api/v2/oauth/token
Authorization: Basic base64(client_id:client_secret)
Content-Type: application/x-www-form-urlencoded

grant_type=refresh_token&refresh_token={현재_refresh_token}
```

응답:
```json
{
  "access_token": "새토큰",
  "refresh_token": "새리프레시토큰",
  "expires_at": "2026-03-25T12:00:00.000",
  "refresh_token_expires_at": "2026-04-08T12:00:00.000"
}
```

---

## 8. refresh_token 만료 알림 흐름

monitoring-gas가 1시간 갱신 시 만료 임박 감지:

```javascript
// monitoring-gas checkCafe24Token()
D-7: 🟡 경고 상태 + sendAlert() 이메일 발송
D-1: 🔴 D-1경고 상태 + sendAlert() 이메일 발송
만료: 🔴 만료 상태 + sendAlert() 이메일 발송

// p7_gas ensureTokenRefreshIfNeeded_()
daysLeft === 7 → notifyAdmin_() 이메일 발송
```

---

## 9. refresh_token 만료 시 재인증 절차

### 8.1 링크 클릭 & 승인

```
https://daesan3833.cafe24api.com/api/v2/oauth/authorize
  ?response_type=code
  &client_id=5TbJGxFqFBOtlYEXoWL47D
  &redirect_uri=https://web-cadalog-ver10.vercel.app/api/auth/callback
  &scope=mall.read_product,mall.write_product
```

→ Cafe24 로그인 → [승인] 버튼 클릭

또는 monitoring-gas 스프레드시트 메뉴: [🛠️ 모니터링] → [토큰 재인증 URL]

### 8.2 code 복사

리다이렉트 URL에서 `code=` 값 복사:

```
https://web-cadalog-ver10.vercel.app/api/auth/callback?code=XXXXXXXXXXXXXXX
                                                               ↑ 이 부분 복사
```

### 8.3 주의: callback route 자동 소비 방지

`src/app/api/auth/callback/route.ts`가 활성 상태이면 브라우저가 callback URL에 도달하는 순간 서버가 `code`를 즉시 소비할 수 있습니다.

수동 재인증(`scripts/reauth_cafe24.py`)을 사용할 때는 아래 중 하나만 선택:

1. callback route를 임시 비활성화한 뒤 `code`를 복사해서 로컬 스크립트로 교환
2. callback route를 사용해 서버에서 직접 교환하고, 같은 `code`를 로컬에서 다시 사용하지 않음

중요:
- 동일 `authorization_code`를 callback route와 로컬 스크립트에서 **중복 교환하면 안 됨**
- `code`는 **1회용**이므로 한 번 소비되면 재사용 불가

### 8.4 AI에게 전달

```
authorization_code: 복사한코드
```

AI 자동 처리: 토큰 교환 → Redis 저장 → Vercel 환경변수 업데이트 → 모니터링 시트 업데이트

주의: code는 발급 후 **1분 이내** 제출

### 8.5 2026-05-06 실제 복구 절차

이번 장애에서는 아래 순서로 복구를 진행함:

1. 기존 `refresh_token`이 `invalid_grant` 상태인지 확인
2. `scripts/reauth_cafe24.py` 기반 수동 재인증으로 새 token 세트 발급
3. 새 token 세트를 아래 3곳에 반영
   - monitoring-gas 시트 `[설정]`
   - Redis `cafe24_tokens`
   - Vercel env fallback (`CAFE24_ACCESS_TOKEN`, `CAFE24_REFRESH_TOKEN`)
4. `monitoring-gas refreshCafe24Token()` 수동 실행으로 갱신 체인 정상 여부 확인
5. callback route 임시 차단 해제 후 Vercel 재배포

확인 포인트:
- `[설정]` 탭의 `TOKEN_EXPIRES_AT`, `REFRESH_EXPIRES_AT`가 새 값인지 확인
- Redis와 Vercel env fallback도 같은 token 세트로 맞춰졌는지 확인
- `checkAll()`이 다시 실행되기 전까지 대시보드의 `🔴만료` 표시는 오래된 값일 수 있음

---

## 9-A. 2026-05-06 장애 원인 분석

### 최초 원인

- `monitoring-gas refreshCafe24Token()` 자동 트리거가 중단됨
- `2026-05-01 12:13` 이후 모니터링 대시보드/로그 갱신 공백 확인
- Google 결제 중단 기간과 실행 공백이 겹쳐, 결제/구독 문제로 Apps Script installable trigger가 멈췄을 가능성이 가장 높음

### 2차 결과

- access_token 자동 갱신 중단
- 기존 `refresh_token`도 이후 `invalid_grant` 상태로 무효화
- 자동 복구 불가 → 수동 재인증 필요

### 운영상 핵심 교훈

- `monitoring-gas`와 `gas-push`를 혼동하면 안 됨
  - 실제 토큰 갱신 단일 주체는 `monitoring-gas`
  - `gas-push`의 `refreshCafe24Token(cfg)`는 내부 헬퍼라 직접 실행 대상이 아님
- `checkAll()`은 대시보드 상태 갱신 함수이고, 현재 코드상 6시간 주기 트리거임
- 따라서 `[설정]` 탭이 갱신돼도 대시보드는 다음 `checkAll()` 전까지 오래된 `🔴만료` 상태를 유지할 수 있음

---

## 10. 에러 코드 대응

| 에러 | 원인 | 대응 |
|---|---|---|
| `401 Unauthorized` | access_token 만료 | 자동 갱신 (tryRefreshAndRetry_) |
| `invalid_grant` | refresh_token 만료 또는 재사용 | 재인증 절차 (8번 섹션) |
| `invalid_client` | CLIENT_ID/SECRET 오류 | 환경변수 확인 |

---

## 11. 운영 요약

| 항목 | 자동 | 수동 |
|---|---|---|
| access_token 갱신 | ✅ monitoring-gas 1시간마다 | - |
| 만료 임박 알림 | ✅ monitoring-gas D-7, D-1 | - |
| refresh_token 갱신 | ❌ (카페24 정책상 자동 불가) | 14일마다 1회 재인증 |

결론: Cafe24 정책상 완전 자동 영구 유지 불가. 현실적 구조 = access_token 자동 갱신 + 만료 전 알림 + 14일마다 1회 재인증.

---

## 11. 2026-05-06 장애 대응 기록

### 장애 원인
Google Workspace 결제 취소(05-01 ~ 05-06) → `monitoring-gas` 트리거 중단 → 5일간 토큰 갱신 중단 → `refresh_token` 만료(`invalid_grant`)

### 복구 절차
1. `monitoring-gas` GAS 편집기에서 `refreshCafe24Token()` 수동 실행 시도 → `invalid_grant` 확인
2. `scripts/reauth_cafe24.py` 로 새 `authorization_code` 교환
   - callback route가 code를 자동 소비하므로 임시 503 비활성화 후 진행
   - `src/app/api/auth/callback/route.ts` 상단에 503 반환 추가 → 배포 → 재인증 → 503 제거 → 재배포
3. 새 토큰을 3곳에 반영
   - `monitoring-gas [설정]` 시트
   - Redis (`/api/token-update`)
   - Vercel 환경변수
4. `monitoring-gas`에서 `refreshCafe24Token()` 수동 실행 → 성공 확인
5. 트리거 재확인 (`refreshCafe24Token / 1시간마다`)

### 추가 발견 문제
`gas-push [설정]` 시트에 `MONITORING_SHEET_ID` 행이 없어서
`syncPrices`가 만료된 토큰으로 실행 중이었음

**해결:** `gas-push [설정]` 시트에 아래 행 추가
| A열 | B열 |
|---|---|
| `MONITORING_SHEET_ID` | `1UnhOYjQmCD-rkYHZv9oP9yuUP0cUJmxV0ERhPK6lpjU` |

### 주의사항
- `monitoring-gas` 대시보드가 🟢 정상이어도 `gas-push` 토큰은 별도로 관리됨
- `gas-push`는 `MONITORING_SHEET_ID` 가 있어야 `monitoring-gas` 최신 토큰을 오버레이받음
- Google Workspace 결제는 반드시 유지할 것 (결제 중단 시 GAS 트리거 중단됨)

### 재발 시 체크리스트
1. `monitoring-gas` 토큰 정상인데 알림 메일 오류 → `gas-push [설정]` 시트 토큰 확인
2. `gas-push [설정]`에 `MONITORING_SHEET_ID` 있는지 확인
3. `authorization_code` 재인증 시 callback route 503 임시 비활성화 후 진행
