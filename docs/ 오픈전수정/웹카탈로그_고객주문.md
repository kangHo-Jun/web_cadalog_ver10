# 웹카탈로그 고객주문 기능 및 장애 기록

## 1. 3/27 개발 내용

- 기능: 웹카탈로그 주문입력 → 구글시트 출력
- 추가 파일:
  - `src/app/api/submit-quote/route.ts`
  - `gas-push/Code.js`
- Redis와 무관한 기능 (GAS Webhook 방식)

## 2. 오류 발생 (2026-03-29)

- 증상: Redis 연결 🔴 / debug-snapshot 데이터 없음
- 원인: 3/27 개발 시 `@upstash/redis` 라이브러리 잘못 도입
  - Upstash 설정 없음
  - 환경변수 없음
  - 더미 데이터로만 테스트 후 납품
- 표면화 시점: Redis 기존 캐시 소멸 후 재생성 시도 → 연결 실패

## 3. 해결 방법

- `@upstash/redis` 제거
- `redis` 라이브러리 방식으로 복원
- `KV_REDIS_URL` 단일 환경변수 사용
- 커밋: `37fb2ef`, 최종커밋

## 4. 재발 방지 원칙

- 신규 라이브러리 도입 시 환경변수 추가 여부 Zart 확인 필수
- 실제 데이터로 검증 후 납품
- 기존 기능 정상 작동 동시 확인 필수
